<h1>Burgers!</h1>

<h2>Burgers That Are Yet To Be Devoured</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burger-block devoured=false}}
  {{/unless}}
  {{/each}}
</ul>


<h2>Burgers That Have Been Devoured</h2>

<ul class="strike">
  {{#each burgers}}
  {{#if devoured}}
  {{> burger-block devoured=true}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bur" name="burger_name">
  </div>

  <div class="form-group" id="devoured">
    <label for="devour">Devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> To Be Eaten<br>
    <input type="radio" name="devoured" value="1"> Devoured
  </div>

  <button type="submit">Add Burger</button>
</form>